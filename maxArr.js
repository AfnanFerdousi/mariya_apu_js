// PROBLEM 3
// create a function
// send an array of numbers inside the function
// calculate the max number of the array

// const arr = [7, 8, 9];
// function arraycheck(arrrrrr) {
//     console.log(arrrrrr)
//     }

// arraycheck(arr)

function maxarr(arr) {
    variable = arr[0];
    run a for loop  
        in th for loop check if the other values of the array is greater than the variable declared before  
    if it is, REPLACE the variable's value with the current value 
}

maxarr([7, 8, 9])